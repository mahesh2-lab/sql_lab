## Packages
@electric-sql/pglite | In-browser Postgres engine for executing SQL
@monaco-editor/react | Code editor for SQL input
react-resizable-panels | Split pane layout for editor/instructions/results
idb | IndexedDB wrapper for saving local progress
canvas-confetti | Celebration effect on successful submission
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
  sans: ["var(--font-sans)"],
}
